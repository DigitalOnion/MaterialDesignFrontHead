<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:background="@color/colorPrimary"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/nav_logo"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintVertical_bias="0.3"
        app:layout_constraintHorizontal_bias="0.15"
        />

    <TextView
        android:id="@+id/leyend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/nav_title"
        android:textColor="@color/colorControlNormalLight"
        android:textSize="30sp"
        app:layout_constraintTop_toBottomOf="@id/nav_logo"
        app:layout_constraintLeft_toLeftOf="@id/nav_logo" />

    <TextView
        android:id="@+id/sub_leyend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/nav_sub_title"
        android:textColor="@color/colorControlNormalLight"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/leyend"
        app:layout_constraintLeft_toLeftOf="@id/leyend" />

</android.support.constraint.ConstraintLayout>